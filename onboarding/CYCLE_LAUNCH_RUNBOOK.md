# 🏁 Runbook запуску циклу (проєкт-giveaway)

> Повторюваний чек-лист для кожного нового авто-циклу. Ціль: запуск не рідше 1/міс.
> Оновлено 10.08.2026. Константи — з research-2026-07 (див. памʼять).

## Стан на 10.08.2026
- Останній цикл у `launches` — **X6M (№20)**, `ends_on=2026-08-03`, досі `status='active'` (фінал був тиждень тому).
- Рядка **№21 ще немає** — тому дашборд/аудит «не знають» про новий цикл.

## 🔴 Рішення Вадима перед стартом №21
1. **Транзиція №20 (X6M):** `active` → `measure` (фаза заміру, як iPhone2) чи `archived`? Впливає на P&L-атрибуцію — тому твоє рішення. Скажи статус — виставлю.
2. **Дані №21:** яке авто, `starts_on`, `ends_on`, `prize_cost_uah`, `code`, `deal_aliases`. Дай — заведу рядок у `launches` (тоді система «побачить» цикл).

## Крок 1 — Завести цикл у систему (я, після даних)
- INSERT у `launches`: name/code/starts_on/ends_on/prize_cost_uah/deal_aliases/color, `status='active'`, `is_active=true`.
- Перевірити, що `detect-unmapped-projects` не алертить (deal_aliases покривають назви угод з еквайрингу).
- Дашборд: проєкт зʼявляється у розділі «Проекти» з власним lifetime-періодом.

## Крок 2 — Медіа-план і реклама (Meta)
Календарне вікно (research-константи):
- **Дні 0-5 від старту = найкращий ROAS** (6.6-8.0, CPA 40-55) — заряджати найсильніші крео саме тут.
- Стеля baseline — **25k/день**; бліц — **макс 1/міс**.
- Беззбитковість по pixel-ROAS = **3.7** (не 2.9); реальна шкала pixel/real = **1.86**.
- VALUE-оптимізація (5.27) > OFFSITE (3.48) — ядро на VALUE.
- **Д-7 до фіналу** — вмикати дожимні крео/адсети (шаблон DC|F).
- Кампанію «На НОВИХ продажі» **не різати за ROAS** (захищена).
- Пауза лише НЕЕФЕКТИВНИХ (ROAS < беззбитковості); прибуткові — ніколи; сумнів → питати Вадима.

## Крок 3 — Ретеншн і органіка
- Серія розсилок у бота: анонс старту → прогрів → Д-7 дожим → фінал (див. RETENTION_BROADCAST_LAUNCH.md).
- SMM-план виходів (watchdog стежить за сторіз/постами).
- База = 70-85% виручки; платного шару дожиму бази немає — тому ретеншн-серія критична.

## Крок 4 — Фінал і закриття
- Автовимкнення реклами о 23:59 дати фіналу (scheduled task, одноразовий на дату).
- Після фіналу: №N → `measure`, ретро+P&L ≤72 год, висновки → у наступний цикл.
- Врахувати висновки попереднього (для №21 — звіт `x6m-report-2026-08/`).

## Автоматизація яка вже стежить
- `dreamcar-daily-creative-audit` (щодня 10:10) — аудит+реалокація Meta.
- `smm_content_watchdog`, `ig-digest`, `kasa-stale-watchdog`, `tech-requests-watchdog`.
- `detect-unmapped-projects` — алерт, якщо угоди нового авто не змаплені на `launches`.
